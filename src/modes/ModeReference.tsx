import { modeRegistry } from './defaults';
export function ModeReference() { const modes = modeRegistry.listModeDefinitions(); return <section className="mode-reference"><p className="eyebrow">MODE REFERENCE</p><h2>Transportation palette</h2>{modes.map((mode) => <article key={mode.id} className="mode-card" style={{ borderLeftColor: mode.color }}><h3><span>{mode.symbol}</span>{mode.name}</h3><p>{mode.summary}</p><dl><dt>Capacity</dt><dd>{modeRegistry.getVehicleDefinitionsForMode(mode.id)[0]?.capacity ?? '-'} riders</dd><dt>Speed</dt><dd>{mode.operations.defaultCruisingSpeedKph} km/h</dd><dt>Fare</dt><dd>${(mode.operations.defaultFare.fareCents / 100).toFixed(2)}</dd><dt>Service</dt><dd>{mode.operations.defaultFrequency.daytimeHeadwayMinutes}/{mode.operations.defaultFrequency.nighttimeHeadwayMinutes} min</dd></dl><p className="vehicle-list">{modeRegistry.getVehicleDefinitionsForMode(mode.id).map((vehicle) => vehicle.name).join(' · ')}</p></article>)}</section>; }
